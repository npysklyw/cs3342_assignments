#include <iostream>
using namespace std;
int main() {
int a ;// if remainder is zero then even number
cin>>a;// if remainder is zero then even number
/* The code below will print the words Hello World!
to the screen, and it is amazing */
if(a%2 == 0)  // if remainder is zero then even number
    cout<<”even”;
else       cout<<”odd”;
return 0;
}
Input: 8
Output: even